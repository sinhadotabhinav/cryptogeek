1. get all symbols and quote assets and print them on the console
curl -X GET "https://api.binance.com/api/v3/exchangeInfo"

2. ask user to select a quote asset
3. ask user to select a base asset(s) (for example, DOGE, XRP, BTC, etc) separated by commas
4. print the latest details
curl -X GET "https://api.binance.com/api/v3/ticker/price"
curl  -X GET "https://api.binance.com/api/v3/ticker/24hr?symbol=ETHBTC"
curl -X GET "https://api.binance.com/api/v3/ticker/price?symbol=DOGEUSDT"
5. print the profit as of today if invested already.
